SELECT
	CAST(LPEP_DROPOFF_DATETIME AS DATE) AS "day",
	MAX(TRIP_DISTANCE)
FROM
	GREEN_TAXI_TRIPDATA
WHERE
	TRIP_DISTANCE < 100
GROUP BY
	CAST(LPEP_DROPOFF_DATETIME AS DATE)
ORDER BY
	MAX(TRIP_DISTANCE) DESC
LIMIT
	100;