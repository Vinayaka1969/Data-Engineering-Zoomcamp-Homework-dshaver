SELECT
	MAX(TIP_AMOUNT),
	Z."Zone"
FROM
	GREEN_TAXI_TRIPDATA G,
	ZONES Z
WHERE
	G."PULocationID" = 74
	AND CAST(LPEP_PICKUP_DATETIME AS DATE) BETWEEN '11-01-2025' AND '12-01-2025'
	AND G."DOLocationID" = Z."LocationID"
GROUP BY
	Z."Zone"
ORDER BY
	MAX(TIP_AMOUNT) DESC