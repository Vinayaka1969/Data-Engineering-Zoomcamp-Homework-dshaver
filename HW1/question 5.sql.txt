SELECT
	Z."Zone",
	SUM(TOTAL_AMOUNT)
FROM
	GREEN_TAXI_TRIPDATA T,
	ZONES Z
WHERE
	CAST(T.LPEP_PICKUP_DATETIME AS DATE) = '11-18-2025'
	AND T."PULocationID" = Z."LocationID"
GROUP BY
	Z."Zone"
ORDER BY
	SUM(TOTAL_AMOUNT) DESC
LIMIT
	100;